import type { Meta, StoryObj } from '@storybook/react'

import ReportPage from './ReportPage'

const meta: Meta<typeof ReportPage> = {
  component: ReportPage,
}

export default meta

type Story = StoryObj<typeof ReportPage>

export const Primary: Story = {}
